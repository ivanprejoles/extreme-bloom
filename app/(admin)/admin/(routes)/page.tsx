import { redirect } from 'next/navigation';

const AdminPage = () => {
  // Perform the server-side redirect
  // redirect('/admin/product'); // Replace with your target destination
  
  return null; // Render nothing since it's being redirected
};

export default AdminPage;
